# pliocene-da
Implements paleoclimate data assimilation for the Pliocene.

### Contents
The `data` folder can be used to upload the input data sets used to run the DA. It has two subfolders, `proxy_records` and `model_output`, to help organize uploaded data.
